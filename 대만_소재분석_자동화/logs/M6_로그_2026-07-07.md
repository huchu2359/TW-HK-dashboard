# M6 로그 — 2026-07-07

## 매칭 테이블 중복탐지
매칭 테이블 19건의 매칭키(브랜드_컨셉명)를 전수 확인 — **중복 없음** (전부 유일값). 향후 M2를 재실행할 때마다 이 검사를 반복 필요.

## 대시보드 신규 구축
- 경로: `대만홍콩 사업팀/대만_소재분석_자동화/dashboard/index.html`
- 별도 서버/빌드 없이 더블클릭으로 브라우저에서 바로 열리는 단일 HTML 파일 (팀 기존 대시보드와 동일한 방식).
- 탭 구성: 종합 / 소재 진단(필터) / 매칭 현황판 / 개선안 검수 큐 / 성과 추이(브랜드 필터)
- 데이터 연동: Notion API 직접 연동이 불가하므로, Claude Code가 Notion MCP로 3개 DB를 pull → HTML 파일 상단의 `DATA` 상수에 직접 기입하는 방식 (수동/반자동 트리거). 최신화가 필요할 때 "대시보드 갱신해줘"라고 요청하면 다시 pull해서 파일을 갱신함.
- JS 문법 검증(node --check) 통과, 브라우저에서 오픈 확인.

## 다음 스냅샷부터 채워질 것
- 성과 추이 탭은 현재 스냅샷 1회차뿐이라 주차별 트렌드 차트는 비어있음. 다음 주 M2~M3 재실행 후 스냅샷이 누적되면 자동으로 트렌드가 쌓임.
